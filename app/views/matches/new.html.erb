<h1>New match</h1>
<p id="notice"><%= notice %></p>
<%
# Get current playerdiv by id
def get_playerdiv()
  playerdiv = Playerdiv.where(:player_id => current_player).last
  return playerdiv
end
%>

<p>In case of a walkover give the player who couldn't make it 'W/O' and yourself 3.</p>

<p>Please press submit just once</p>
<%= render 'form' %>

<%= link_to 'Back', matches_path %>
