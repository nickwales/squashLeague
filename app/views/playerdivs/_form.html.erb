<%= form_for(@playerdiv) do |f| %>
  <% if @playerdiv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playerdiv.errors.count, "error") %> prohibited this playerdiv from being saved:</h2>

      <ul>
      <% @playerdiv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :division_id %><br />
    <%= f.number_field :division_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
